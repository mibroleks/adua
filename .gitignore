# Logs and system files
*.log
.DS_Store
Thumbs.db

# Environment files (never commit secrets)
.env
.env.backup
.env.production

# IDE/editor configs
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json

# Node modules (too large, build locally)
node_modules

# Keep build artifacts (so they deploy to Namecheap)
# REMOVE /public/build from ignore
# REMOVE /vendor from ignore

# Ignore only dev-specific files
/public/fonts-manifest.dev.json
/public/hot
/public/storage

# Storage keys and temp files
/storage/*.key
/storage/pail

# Laravel helper files
_ide_helper.php
Homestead.json
Homestead.yaml
